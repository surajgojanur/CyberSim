# Screenshot Placeholders

Add real screenshots here before publishing or submitting the project.

Recommended captures:

- `admin-lobby.png`: admin session lobby with join code and participant list
- `participant-join.png`: participant join page
- `active-round.png`: participant active question round
- `reveal.png`: reveal state with correct answer, explanation, and leaderboard
- `final-results.png`: final results page

Do not use mock screenshots; capture the running app after `npm run dev`.
